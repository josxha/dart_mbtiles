name: mbtiles
description: Mapbox MBTiles v1.3 files, support for vector and raster tiles.
version: 0.5.1
repository: https://github.com/josxha/dart_mbtiles
issue_tracker: https://github.com/josxha/dart_mbtiles/issues
topics: [ mbtiles, map, mvt ]

platforms:
  android:
  ios:
  linux:
  macos:
  windows:

environment:
  sdk: ^3.9.0

dependencies:
  latlong2: ^0.10.0
  meta: ^1.10.0
  sqlite3: ^3.1.6

dev_dependencies:
  lint: ^2.2.0
  path: ^1.8.3
  test: ^1.24.9